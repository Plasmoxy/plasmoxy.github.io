/* uses p5.js | by Plasmoxy | @template=gui */

let canvas, bgColor;

function setup() {
  bgColor = color(10);
  canvas = createCanvas(600, 300);
  canvas.parent('canvasroot');



}

function draw() {
  background(bgColor);

}
