/* uses p5.js | by Plasmoxy | @template=fullscreen */

let canvas, bgColor;

function setup() {
  bgColor = color(10);
  canvas = createCanvas(windowWidth, windowHeight);



}

function draw() {
  background(bgColor);

}
